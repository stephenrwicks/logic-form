JsonLogic form generator function - work in progress

TS function that generates a plain html form from JSON config and allows complex rules for interdependent field log. For example make textbox A required if checkbox B is checked, show textbox C if select element D is set to a certain option, etc.

~ March / April 2026
